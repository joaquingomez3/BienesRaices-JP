@model bienesraices.Models.Usuario
@{
    ViewData["Title"] = "Iniciar Sesión";
    ViewData["OcultarHeader"] = true;
}

<div class="container mt-3">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="text-center mb-4">
                <img src="~/imagenes/logo.png" alt="Bienes Raíces JP" style="max-width:150px;">
            </div>

            <div class="card shadow-lg border-0 rounded-3">
                <div class="card-header text-center py-3" style="background-color:#bb4900;">
                    <h3 class="mb-0 text-white">🔑 Iniciar Sesión</h3>
                </div>
                <div class="card-body">
                    @if (ViewBag.Error != null)
                    {
                        <div class="alert alert-danger text-center">
                            @ViewBag.Error
                        </div>
                    }

                    <form asp-controller="Usuario" asp-action="Login" method="post">
                        <div class="mb-3">
                            <label for="email" class="form-label">Correo electrónico</label>
                            <input type="email" class="form-control" id="email" name="email"
                                placeholder="ejemplo@correo.com" required>
                        </div>

                        <div class="mb-3">
                            <label for="clave" class="form-label">Contraseña</label>
                            <input type="password" class="form-control" id="password" name="password"
                                placeholder="********" required>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary"
                                style="background-color:#bb4900; border:none;">
                                <i class="bi bi-box-arrow-in-right me-1"></i> Ingresar
                            </button>
                        </div>
                    </form>


                </div>
            </div>
        </div>
    </div>
</div>
<div class="container mt-5 d-flex justify-content-center">
    <div class="card shadow-lg p-3 rounded-4" style="max-width: 500px; width: 100%;">
        <div class="card-header text-white text-center rounded-3"
            style="background: linear-gradient(90deg, #bb4900, #8a2e00);">
            Credenciales de acceso para el profesor
        </div>
        <div class="card-body p-0">
            <table class="table table-hover text-center mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Rol</th>
                        <th>Correo</th>
                        <th>Contraseña</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Admin</td>
                        <td>admin@inmobiliaria.com</td>
                        <td>Admin1234</td>
                    </tr>
                    <tr>
                        <td>Empleado</td>
                        <td>joaquin@mail.com</td>
                        <td>123</td>
                    </tr>
                    <tr></tr>
                    <td>Empleado</td>
                    <td>pablo@mail.com</td>
                    <td>1234</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>
